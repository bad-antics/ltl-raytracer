// examples/demo.ltl — runnable demo for ltl_raytracer

import std::io
use ltl_raytracer as lib

fn main() {
    io::println("ltl_raytracer demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}